describe('tests for', () => {
    it('should work, counting match groups', () => {
        const str: string = '(__(.*)__)';
        const countReqExpMatchGroups = (reqexpStr: string) => {
            const parentheses = '';
            return null;
        };
    });
});
